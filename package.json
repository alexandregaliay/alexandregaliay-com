{
		"name": "alexandregaliay-com",
		"version": "1.0.0",
		"license": "MIT",
		"repository": "https://github.com/alexandregaliay/alexandregaliay-com.git",
		"author": "alexandregaliay <dev@alexandregaliay.com>",
		"main": "src/index.html",
		"scripts": {
				"serve": "parcel index.html --open",
        "play": "parcel play.html --open",
        "build": "parcel build *.html",
				"clean": "rm -rf dist/*",
				"rebuild": "yarn run clean && yarn run build"
		},
		"devDependencies": {
				"sass": "^1.27.0"
		}
}
